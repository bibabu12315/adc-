# ADC 数据分析图表阅读指南

以下是程序成功运行后自动为您生成的 4 类图表的详细解读指南，这 4 张图从不同维度分析了 ADC（模数转换器）采集到了的信号质量。

---

### 第一张图：`_adc_waveform.png` (ADC 值时间序列波形图)

**1. 它是什么？**
将采集到的所有 ADC 原始数值按照采集时间的先后顺序连成的一根连续的曲线。也就是最直观的“波形图”。

**2. 坐标轴含义：**
- **X 轴 (sample index)**：采样点索引，通常是从 0 到 10000（这是您输入的总数据量）。相当于时间轴。
- **Y 轴 (ADC value)**：每个采样点所采集到的原始模数结果（例如 12位 ADC 的范围是 0 到 4095）。

**3. 该怎么看？**
- **如果输入信号是直流 (DC) 常数**：理想情况下，线应该是水平的直线。但实际上因为有噪声存在，这条线会出现一定幅度的上下“毛刺”（采样抖动）。毛刺的宽度（峰峰值）就是系统在此刻的底噪表现。
- **如果输入信号是交流信号 (例如正弦波)**：您应该能明显看到周期性的波形。如果波形出现毛刺、削顶或由于采样率不够造成的失真，在这张图中都能直观发现。
- **看突刺和异常**：如果线上突然出现了极端的偏离（偶尔跳一个特别大或小的指），说明可能存在突发的干扰噪声或者 SPI/I2C 总线死锁误码。

---

### 第二张图：`_adc_histogram.png` (ADC 采样值分布直方图)

**1. 它是什么？**
对所有采样的数据进行统计归类，统计每一种数值出现了多少次。这种图在统计学上叫做“直方图”（Histogram）或者“频数分布图”。

**2. 坐标轴含义：**
- **X 轴 (ADC value)**：ADC 采样后得到的值。
- **Y 轴 (count)**：该 ADC 值在所有采样中出现的总次数（频数）。

**3. 该怎么看？**
如果您输入的是一个非常稳定的直流电压信号（比如固定的 1.2V 基准）进行噪声测试：
- **完美的系统**：应该只有一根高高的柱子，说明 10000 次都采到了同一个值。
- **正态分布**：现实中，柱子通常呈现“中间高、两边低”的“钟形”或“高斯正态分布”。这个钟形的宽度越窄，说明噪声越小（标准差 `std` 越小）；宽度越宽，说明数据分散，也就是噪声较大。
- **多峰分布**：如果出现了两座或多座高高的“山峰”（双峰分布），说明您的系统可能存在强烈的固定频率周期性干扰（比如市电 50Hz 工频干扰），或者是某个引脚或布线引入了确定的数字噪声。
- **缺失码 (Missing Codes) 或微分非线性 (DNL) 差**：如果发现有的 ADC 值柱子突然“凹进去了”（在这个范围内明明应该有数据但是出现次数是 0 或者是极小的值），说明这个 ADC 芯片的线性度可能有问题（某几个特定数值很难转换出来）。

---

### 第三张图：`_adc_fft.png` (FFT 噪声频谱图)

**1. 它是什么？**
将时域的波形（第一张图）通过“快速傅里叶变换 (FFT)”转化到频域，展示信号包含了哪些不同频率成分以及它们的强度。在绘制前，程序已经帮您过滤了直流成分（除去了平均值中心），以便看清细微的交流噪声。

**2. 坐标轴含义：**
- **X 轴 (frequency (Hz))**：信号中携带的不同交流频率成分。如果您配置中采样率 `--fs` 设置为 20kHz（20000 Hz），根据奈奎斯特采样定理，能观察到的最大频率（横坐标的最右端）就是 10kHz（10000 Hz）。
- **Y 轴 (magnitude)**：对应的该频率成分在这个系统中具有的强度或幅度。

**3. 该怎么看？**
在测试直流静电或本底噪声的情况下：
- **理想状态**：底部分布着均匀的超低幅度噪声底线（“白噪声”）。没有任何一个频率会产生突出的尖峰。
- **寻找干扰源的利器**：
  - 如果在 **50Hz** 或 **100Hz** 附近有一根非常突出的针（尖峰），这是电源线未做好屏蔽，引入了 220V 交流工频干扰的最典型特征。
  - 如果在 **几k Hz 或更高** 出现的明显尖峰，通常是板子上的开关电源（DC-DC）、背光驱动或其它的时钟振荡器辐射过来的开关噪声。
  - 这个图是一把“放大镜”，它是抓捕系统高频周期性幽灵噪声的杀手锏。

---

### 第四张图：`_adc_bit_noise.png` (Bit 翻转概率/位抖动图)

**1. 它是什么？**
重点关注 ADC 这串二进制数据里，每一位数字（从最不重要的右边到最重要左边）到底有多稳定。程序是这么算的：在连续的两次采样中，比如上一个是 1，下面如果变成了 0（发生翻转），这就记作一次抖动事件。我们计算出位被翻转的概率。

**2. 坐标轴含义：**
- **X 轴 (bit index)**：数据的位。比如 12 位 ADC，它就是 `0` 到 `11`。其中 0 是最低位 LSB (Least Significant Bit)，11 是最高位 MSB (Most Significant Bit)。
- **Y 轴 (flip probability)**：翻转的概率。它的范围理论上是 0 ~ 1.0，但大多数噪声都是在 0 到 0.5 之间。

**3. 该怎么看？**
这张图主要用来直观地评估**由于噪声，ADC 到底“丢掉了”多少有效位数**，也是验证最终 ENOB（有效位数）是否合理的重要手段。
- **接近 0 的概率**：柱子几乎看不见，说明这个位极其稳定。正常信号的变化或本底噪声都触发不了这一位的变化（如高位的 MSB 肯定是坚若磐石的）。
- **接近 0.5 的概率（50%）**：说明这个位是“纯随机”瞎跳的。抛个硬币，正反面都是 50%，当柱子跑到 0.5 时，证明这一位的 0 还是 1 完全被周围的“白噪声”淹没，不包含任何真实的输入电压信息，这被称为**陷入底噪**。
- **从右往左看（从低位到高位）**：一般最低的几位（如 bit0, bit1, bit2）的翻转概率会逼近 50%，说明它们都是无效噪声。随着 bit index 升高，柱子会像楼梯一样急剧下降并稳定为接近 0。如果一个名义上标着 12位（bit 0~11）的 ADC 发现有 3 位的概率都摸到了 0.5，这说明其有效信号只能由上面稳固的 9 位提供，它的真实有效位数大概也就只剩 9 位了。